# Uncomment the next line to define a global platform for your project
platform :ios, '16.0'

target 'MoonCaptureApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MoonCaptureApp
  pod 'OpenCV', '~> 4.0'

  target 'MoonCaptureAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MoonCaptureAppUITests' do
    # Pods for testing
  end

end
